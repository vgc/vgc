/* Default style for buttons and labels */
Button,
Label {
    margin-top: 10dp;
    margin-right: 10dp;
    margin-bottom: 10dp;
    margin-left: 10dp;
}

/* Default style for flex container.
   Note: these values are temporary (for testing only). */
Flex {
    margin-top: 5dp;    /* temporary value */
    margin-right: 5dp;  /* temporary value */
    margin-bottom: 5dp; /* temporary value */
    margin-left: 5dp    /* note that the last semicolon of a declaration block is optional */
}
